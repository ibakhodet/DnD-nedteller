# Project conventions for Claude Code

## Git workflow

Push changes directly to `main`. Do not create feature branches or pull
requests unless explicitly asked. This is a personal project — fast-forward
commits to `main` is the preferred flow.
